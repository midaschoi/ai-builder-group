-- ═══════════════════════════════════════════════════════════════════════════
-- 0008 · 구글 태그 관리자(GTM) 컨테이너 ID
--
-- 왜 DB 인가 —
--   2차 미팅 04:22 "관리자 페이지에서 링크만 딱 바꾸면 렌더링 되게끔만".
--   A-08 의 다른 값(pluug · GA4 · 서치콘솔)과 같은 규칙을 따른다.
--   코드에 박으면 바꿀 때마다 배포해야 하고, 이관 후에는 손댈 수 없다.
--
-- ⚠ GA4 측정 ID(G-…) 와 **다른 값**이다. 둘은 역할이 다르다:
--     ga4_measurement_id  G-XXXXXXXXXX   측정 그 자체
--     gtm_container_id    GTM-XXXXXXX    태그를 담는 그릇. 안에 GA4 태그를 또 만들어야 한다
--
--   ⛔ 둘 다 GA4 를 연결하면 **같은 이벤트가 두 번 집계된다.**
--      한쪽만 쓴다 — 자세한 건 components/GoogleTagManager.tsx 주석.
-- ═══════════════════════════════════════════════════════════════════════════

alter table public.site_settings
  add column if not exists gtm_container_id text;
